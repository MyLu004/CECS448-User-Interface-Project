{
  "name": "my-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -n FE,API -c auto \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "start": "npm run start -w backend"
  },
  "workspaces": ["frontend", "backend"],
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
